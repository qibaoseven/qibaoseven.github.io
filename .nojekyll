# This file tells GitHub Pages to skip Jekyll processing
# Without Jekyll, all files in the repository (including those starting with underscores)
# will be served as static content. This prevents issues with certain file patterns.